/**
 * Companies — barrel export
 */
export {
  normalizeCompany,
  cleanCompanyName,
  buildAliasMap,
  CANONICAL_COMPANIES,
} from './normalize'

export type {
  CanonicalEntry,
  NormalizationResult,
} from './normalize'
